# Core MCP server framework
mcp>=1.0.0

# Async subprocess + SQLite are stdlib (asyncio, sqlite3) — no extra deps needed

# Optional, for gpt_sol / deepseek runners if calling their APIs directly
# instead of shelling out to a CLI (finalize once each provider's official
# CLI/SDK is confirmed — see docs/RUNNER_SPEC.md open question):
# openai>=1.0.0
# deepseek-sdk  # placeholder — replace with actual package name once confirmed
